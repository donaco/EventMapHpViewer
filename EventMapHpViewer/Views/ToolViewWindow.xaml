<Window x:Class="EventMapHpViewer.Views.ToolViewWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:views="clr-namespace:EventMapHpViewer.Views"
		xmlns:viewModels="clr-namespace:EventMapHpViewer.ViewModels"
		mc:Ignorable="d"
		d:DataContext="{d:DesignInstance viewModels:ToolViewModel}"
		Title="HP/TPバー"
		Width="800"
		Height="430"
		FontSize="12"
		Background="{DynamicResource ThemeBrushKey}"
		Foreground="{DynamicResource ActiveForegroundBrushKey}"
		FontFamily="{DynamicResource DefaultFontFamilyKey}"
		SnapsToDevicePixels="True"
		Topmost="{Binding IsTopMost, Mode=TwoWay}"
		TextOptions.TextFormattingMode="Display">
	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="../Styles/PluginStyle.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<DockPanel Grid.Row="0"
				   Margin="8,6,8,4">
			<CheckBox Content="常に最前面に表示"
					  IsChecked="{Binding IsTopMost, Mode=TwoWay}"
					  VerticalAlignment="Center" />
			<Button Content="スクリーンショット"
					Click="ScreenshotButton_Click"
					HorizontalAlignment="Right"
					DockPanel.Dock="Right"
					Padding="10,4"
					Margin="8,0,0,0" />
		</DockPanel>

		<views:ToolView Grid.Row="1"
						Margin="4" />
	</Grid>
</Window>

